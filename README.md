# SaaS Subscription Boilerplate

This is a production-ready foundation for SaaS applications, built with **Django REST Framework** and **Next.js**. It features a custom user model, JWT authentication, and a tiered subscription logic.

## 🚀 Current Progress: Sprint 1

I am currently working on the core authentication system and the base architecture.

### Tech Stack

- **Backend:** Django 5.x / Django REST Framework
- **Auth:** SimpleJWT
- **Database:** PostgreSQL (Development: SQLite)
- **Workflow:** Agile (Sprints) with Feature Branching [cite: 2025-12-21].

## 🛠 Installation & Setup

1. Clone the repository.
2. Create a virtual environment: `python -m venv venv`.
3. Activate it and install dependencies: `pip install -r requirements.txt`.
4. Run migrations: `python manage.py migrate`.
5. Start the server: `python manage.py runserver`.
